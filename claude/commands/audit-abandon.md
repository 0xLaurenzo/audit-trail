---
description: Archive an unpublishable audit as abandoned without closing it as complete
---
Call the audit_abandon tool from the audit-trail MCP server. Parse "$ARGUMENTS" as the exact task name and a required --reason <text>; pass them as task and reason. Report the tool output verbatim.
